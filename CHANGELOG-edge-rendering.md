# 更新日志 — 边缘渲染与实时发布修复

> 2026-06-17 | 围绕"CMS 提交后内容秒级可见"的一轮集中修复

## 修复的问题

- 修复所有论文/动态/项目详情页打开显示 Not Found 的问题(URL 与文件夹名不一致导致)
- 修复详情页加载失败时无法回退到静态页、反复 404 的问题
- 修复页面样式错乱(CSS 报 MIME type 错误)——改为渲染时自动获取最新样式文件,不再因构建变动而失效
- 修复 GitHub Action 构建失败(Hugo 版本号写错、wrangler 命令过时、脚本漏提交)
- 修复中文标题新建文章时报"malformed path component"无法保存的问题
- 修复新发布文章在列表页不显示的问题(时区导致被误判为"未来日期")
- 修复两篇标题相同的文章会撞同一网址、互相覆盖的问题——网址改由「URL 标识」决定,各篇唯一
- 修复成果动态(post)网址带日期前缀导致 URL 标识查重失效的问题——post 改为与论文/项目一致的命名(网址不再带日期),查重对 post 同样生效

## 新增能力

- **编辑已有内容秒级可见**:CMS 保存后约 10 秒内刷新即可看到新内容,不再等整站构建
- **新建文章秒级可见**:新文章详情页发布即可访问、分享,无需等待 40-60 秒构建
- 新建文章「URL 标识」字段:决定网址末段(如 `/publication/trvp/`),网址更简洁、可控
- 现有文章网址统一为简短英文(如 TRVP 论文页从一长串标题变为 `/publication/trvp/`)
- 列表页"最新发布"横幅:新文章发布后,列表页顶部立即出现入口链接(指向秒级可见的详情页),不必等整站构建;构建完成后横幅自动消失,文章进入正式列表
- URL 标识查重:新建文章填「URL 标识」时实时检查是否已被占用,撞名即标红、阻止保存(避免新文章因撞名无法显示);另有构建巡检兜底
- 论文著作可在 CMS 添加 BibTeX 引用:新建论文时「BibTeX 引用」字段可**直接粘贴** bib 文本,或**导入 .bib 文件**(读入内容),随论文一起保存;无需再本地手动建 cite.bib
- 论文详情页 Cite 按钮改为「点击复制 BibTeX 到剪贴板」(原为新标签打开文件):内容已内联在页面,点一下即复制,可直接粘进文献管理器(边缘渲染版 + Hugo 静态页版均已生效)

## 使用须知

- 新建论文/动态/项目时,「URL 标识」必填,用英文小写短名(如 `my-paper`),**每篇必须唯一**(字母数字连字符)
- 网址由「URL 标识」决定,与标题无关——标题可随意改、可重复,不影响网址
- 详情页秒级可见;列表页、首页仍随整站构建更新(约 40-60 秒后出现),属正常现象
- CMS 后台改版后请硬刷新 `/admin/` 页面(Ctrl+Shift+R)
- 注:本次现有文章网址变更后,旧网址(基于标题的长串)将失效
- 注:成果动态(post)网址不再带日期前缀(如 `/post/vista/`),日期仍在文章内、用于排序显示;现有 post 旧网址(带日期)同步失效
